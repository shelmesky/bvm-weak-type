contract mycnt {
    func myfunc0(b) {
        var x = b
        return x
    }

    func myfunc1(a, c) {
        var r = myfunc0(333)
        return a+c+r
    }

    var y = myfunc1(111, 222)
    println(y)

    var m = 111
    var n = 222

    if m > n {
        var i = 555555
        println(i)
    } elif m < n {
        var j = 666666
        println(j)
    } else {
        var l = 777777
        println(l)
    }

    var loop = 0
    while (loop<10) {
        println(loop)
        loop += 1
    }

    var t = (1+2)*3/4- 5*6 % 2
    println(t)

    var mystr = "mystr"
    println(mystr)

    var v = 1
    v += 3 * (5 - 1)
    println(v)
}