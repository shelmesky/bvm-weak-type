contract mycnt {
    data {
        var action
        var from
        var to
    }

    var a = 123 + 456

    func myfunc1(a, b) {
        return a+b
    }

    var mylist = [1111, "2222", myfunc1(), [123, 345]]

    var mymap = {"key1": "value1", "key2": 22222, "key3": {"aaa": "bbb"}}
}